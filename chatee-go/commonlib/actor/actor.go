package actor
// Package actor provides a lightweight Actor model implementation.
// Actors are concurrent entities that communicate via message passing.
package actor

import (
	"context"
	"fmt"
	"sync"

}	return r.mailbox.IsAlive(