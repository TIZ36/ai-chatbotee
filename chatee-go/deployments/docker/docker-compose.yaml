version: '3.8'

services:
  # Infrastructure services
  mysql:
    image: mysql:8.0
    container_name: chatee-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-chatee_root}
      MYSQL_DATABASE: chatee
      MYSQL_USER: chatee
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-chatee_pass}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init/mysql:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  redis:
    image: redis:7-alpine
    container_name: chatee-redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-chatee_redis}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-chatee_redis}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  # Chatee services
  dbc_rpc:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
      args:
        SERVICE: dbc_rpc
    container_name: dbc_rpc
    environment:
      - CHATEE_MYSQL_HOST=mysql
      - CHATEE_MYSQL_PORT=3306
      - CHATEE_MYSQL_USER=chatee
      - CHATEE_MYSQL_PASSWORD=${MYSQL_PASSWORD:-chatee_pass}
      - CHATEE_MYSQL_DATABASE=chatee
      - CHATEE_REDIS_HOST=redis
      - CHATEE_REDIS_PORT=6379
      - CHATEE_REDIS_PASSWORD=${REDIS_PASSWORD:-chatee_redis}
      - CHATEE_LOG_LEVEL=info
    ports:
      - "9091:9090"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9090"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  svr_rpc:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
      args:
        SERVICE: svr_rpc
    container_name: svr_rpc
    environment:
      - CHATEE_MYSQL_HOST=mysql
      - CHATEE_MYSQL_PORT=3306
      - CHATEE_MYSQL_USER=chatee
      - CHATEE_MYSQL_PASSWORD=${MYSQL_PASSWORD:-chatee_pass}
      - CHATEE_MYSQL_DATABASE=chatee
      - CHATEE_REDIS_HOST=redis
      - CHATEE_REDIS_PORT=6379
      - CHATEE_REDIS_PASSWORD=${REDIS_PASSWORD:-chatee_redis}
      - CHATEE_LLM_DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - CHATEE_LLM_OPENAI_API_KEY=${OPENAI_API_KEY}
      - CHATEE_LOG_LEVEL=info
    ports:
      - "9092:9090"
    depends_on:
      dbc_rpc:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9090"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  im_rpc:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
      args:
        SERVICE: im_rpc
    container_name: im_rpc
    environment:
      - CHATEE_MYSQL_HOST=mysql
      - CHATEE_MYSQL_PORT=3306
      - CHATEE_MYSQL_USER=chatee
      - CHATEE_MYSQL_PASSWORD=${MYSQL_PASSWORD:-chatee_pass}
      - CHATEE_MYSQL_DATABASE=chatee
      - CHATEE_REDIS_HOST=redis
      - CHATEE_REDIS_PORT=6379
      - CHATEE_REDIS_PASSWORD=${REDIS_PASSWORD:-chatee_redis}
      - CHATEE_LOG_LEVEL=info
    ports:
      - "9093:9090"
    depends_on:
      dbc_rpc:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9090"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  conn_rpc:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
      args:
        SERVICE: conn_rpc
    container_name: conn_rpc
    environment:
      - CHATEE_REDIS_HOST=redis
      - CHATEE_REDIS_PORT=6379
      - CHATEE_REDIS_PASSWORD=${REDIS_PASSWORD:-chatee_redis}
      - CHATEE_LOG_LEVEL=info
    ports:
      - "8081:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "8080"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

  chatee_http:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
      args:
        SERVICE: chatee_http
    container_name: chatee_http
    environment:
      - CHATEE_HTTP_PORT=8080
      - CHATEE_GRPC_DBC_ADDR=dbc_rpc:9090
      - CHATEE_GRPC_SVR_ADDR=svr_rpc:9090
      - CHATEE_GRPC_MSG_ADDR=im_rpc:9090
      - CHATEE_LOG_LEVEL=info
    ports:
      - "8080:8080"
    depends_on:
      dbc_rpc:
        condition: service_healthy
      svr_rpc:
        condition: service_healthy
      im_rpc:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatee-network

networks:
  chatee-network:
    driver: bridge

volumes:
  mysql_data:
  redis_data:
